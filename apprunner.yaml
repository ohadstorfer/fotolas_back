version: 1.0
runtime: python311
run:
  runtime-version: 3.11
  pre-run:
    - pip3 install -r requirements.txt  # Install dependencies here
  command: gunicorn myprog.wsgi:application --bind 0.0.0.0:8000  # Start your application with gunicorn
  network:
    port: 8000